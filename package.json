{
  "name": "barateza-ticket-notifier",
  "version": "3.0.0",
  "description": "Zendesk ticket monitor Chrome extension with code coverage",
  "main": "background.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "coverage:report": "jest --coverage --coverageReporters=html --coverageReporters=text --coverageReporters=json-summary"
  },
  "keywords": [
    "zendesk",
    "chrome-extension",
    "notifications",
    "ticket-monitoring"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@babel/preset-env": "^7.23.0",
    "@testing-library/dom": "^9.3.4",
    "@testing-library/jest-dom": "^6.1.5",
    "babel-jest": "^29.7.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
